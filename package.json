{
  "name": "examina",
  "private": true,
  "scripts": {
    "dev:api": "corepack pnpm --filter api dev",
    "dev:web": "corepack pnpm --filter web dev",
    "dev:mobile": "corepack pnpm --filter mobile start"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
